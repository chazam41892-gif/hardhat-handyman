# GitHub Actions Workflow - Auto-deploy to Cloudflare Pages
# Place this file at: .github/workflows/deploy.yml

name: Deploy to Cloudflare Pages

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    name: Deploy to Cloudflare Pages
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          submodules: false
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          
      - name: Install dependencies
        run: npm install
        
      - name: Install Wrangler
        run: npm install -D wrangler@^4
        
      - name: Deploy to Cloudflare Pages
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          command: pages deploy . --project-name=hardhat-handyman
          wranglerVersion: '4.67.0'
          
      - name: Deployment notification
        run: |
          echo "✅ Site deployed successfully!"
          echo "🌐 Live at: https://hardhat-handyman.com"
