name = "hardhat-handyman"
type = "webpack"
compatibility_date = "2026-02-01"

# Site configuration - bucket points to root since this is a simple static site
# For projects with build tools, change this to your build output directory (e.g., "./dist")
[site]
bucket = "./"

[env.production]
name = "hardhat-handyman-production"
